# Secure-VLab-Gateway
The entire infrastructure was designed and virtualized over a Type-2 hypervisor, segregating Layer 2 traffic through distinct virtual switches (`VMnets`) to guarantee absolute perimeter control and isolation managed by the
